<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List a Comments</title>
</head>
<body>
    <h1>List a Comments</h1>
    <ul>
        <% for(let comment of comments) { %>
            <li> <%= comment.text %> - <%= comment.username %> 
                <a href="/comments/<%= comment.id  %>">Details</a>
                <a href="/comments/<%= comment.id  %>/edit">Edit</a>
                <form action="/comments/<%= comment.id %>?_method=DELETE" method="POST">
                    <button>Delete</button>
                </form>
            </li>
        <% } %>
    </ul>
    <a href="/create">Add</a>
</body>
</html>